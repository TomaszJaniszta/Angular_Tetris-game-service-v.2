import { MyScoresPipePipe } from './my-scores-pipe.pipe';

describe('MyScoresPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MyScoresPipePipe();
    expect(pipe).toBeTruthy();
  });
});
